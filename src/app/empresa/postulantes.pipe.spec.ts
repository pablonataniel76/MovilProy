import { PostulantesPipe } from './postulantes.pipe';

describe('PostulantesPipe', () => {
  it('create an instance', () => {
    const pipe = new PostulantesPipe();
    expect(pipe).toBeTruthy();
  });
});
